@model IEnumerable<DrugsSystem.Models.Database.DrugType>

@{
    ViewBag.Title = "List";
}

<h2>List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DrugTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DrugTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DrugTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DrugTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DrugTypeID })
        </td>
    </tr>
}

</table>
